# URL of the Streamlit chat app, embedded via <iframe> on the dashboard page.
# Local dev default matches `streamlit run app.py --server.port 8503` from the repo root.
NEXT_PUBLIC_STREAMLIT_URL=http://localhost:8503
